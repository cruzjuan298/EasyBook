import { API_CONFIG } from "./api";
import { APP_CONSTANTS } from "./constant";
import  { ROUTE_PATHS } from "./routePaths"

const config = {
    api: API_CONFIG,
    constants: APP_CONSTANTS,
    routes : ROUTE_PATHS,
    appName: "BookmeFrontend",
    verison: "0.1.0"
};
export default config;